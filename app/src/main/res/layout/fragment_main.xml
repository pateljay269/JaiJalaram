<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:activity="patel.jay.jaijalaram.Login.MainFragment">

    <include layout="@layout/sdv_image" />

    <Button
        android:id="@+id/btnSignIn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="@dimen/main_btn_bottom"
        android:layout_marginEnd="@dimen/main_btn_side"
        android:background="@drawable/btnback"
        android:gravity="center"
        android:minHeight="@dimen/min_height"
        android:minWidth="@dimen/min_width"
        android:text="@string/sign_in"
        android:textColor="@android:color/white"
        android:textSize="15sp" />

    <Button
        android:id="@+id/btnSignUp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="@dimen/main_btn_bottom"
        android:layout_marginStart="@dimen/main_btn_side"
        android:background="@drawable/btnback"
        android:gravity="center"
        android:minHeight="@dimen/min_height"
        android:minWidth="@dimen/min_width"
        android:text="@string/sign_up"
        android:textColor="@android:color/white"
        android:textSize="15sp" />

</RelativeLayout>
